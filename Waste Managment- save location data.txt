executeQuery("DELETE FROM PMT_WASTE_SITE_LIST");
foreach (@=waste_management as $row) {
	if (is_array($row) and count($row) != 0) {
		executeQuery("INSERT INTO PMT_WASTE_SITE_LIST VALUES " .
		   "(' '  , '{$row['LOCATION_NUMBER']}', '{$row['LOCATION_NAME']}')");
	}
}